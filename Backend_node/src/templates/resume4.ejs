<!DOCTYPE html>
<html>
<head>
<style>
body{background:#000;color:#e5e7eb;font-family:Montserrat;padding:50px}
h1{color:#22c55e}
.section{margin-bottom:35px}
.header{display:flex;align-items:center;gap:25px;margin-bottom:40px}
.avatar{width:120px;height:120px;border-radius:50%;object-fit:cover;
border:3px solid #22c55e}
.meta p{margin:4px 0}
</style>
</head>
<body>

<!-- Header with Avatar -->
<div class="header">
  <img class="avatar" src="<%= data.avatar %>" alt="Avatar">
  <div class="meta">
    <h1><%= data.name %></h1>
    <p><%= data.title %></p>
    <p>📧 <%= data.email %></p>
    <p>📍 <%= data.location %></p>
  </div>
</div>

<!-- About -->
<div class="section">
  <h2>About</h2>
  <p><%= data.summary %></p>
</div>

<!-- Projects -->
<div class="section">
  <h2>Projects</h2>
  <% data.projects.forEach(p=>{ %>
    <p><b><%= p.title %></b> — <%= p.tech_stack.join(", ") %></p>
    <p><%= p.description %></p>
  <% }) %>
</div>

<!-- Skills -->
<div class="section">
  <h2>Skills</h2>
  <% data.skills.forEach(s=>{ %>
    <span style="display:inline-block;background:#052e16;color:#22c55e;
    padding:6px 12px;border-radius:10px;margin:4px;font-size:12px;">
      <%= s %>
    </span>
  <% }) %>
</div>

</body>
</html>
